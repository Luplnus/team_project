<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/schema/mybatis-3-mapper.dtd">

<!--  -->
<mapper namespace="com.bitstudy.app.dao.userMapper">
    <insert id="insertUser" parameterType="UserDto">
        insert into user_tbl (u_id, u_pw, u_name, u_email, u_address, u_cellnum)
        values (#{u_id}, #{u_pw}, #{u_name}, #{u_email}, #{u_address}, #{u_cellnum})
    </insert>

    <select id="selectUser" parameterType="String" resultType="UserDto">
        select * from user_tbl
        where u_id=#{u_id}
    </select>

    <select id="selectUserSeqnoById" parameterType="String" resultType="int">
        select u_seqno
        from user_tbl
        where u_id = #{u_id}
    </select>

    <delete id="deleteUser" parameterType="String">
        delete from user_tbl
        where u_id=#{u_id}
    </delete>

    <update id="updateUser" parameterType="UserDto">
        update user_tbl
            set u_name=#{u_name}, u_address=#{u_address}, u_cellnum=#{u_cellnum}, u_email=#{u_email}
                where u_id=#{u_id};
    </update>
</mapper>